# Токен не обязателен, но без него GitHub даёт всего 60 запросов в час на IP.
# С токеном — 5000 запросов в час. Создать токен (без единого разрешения,
# просто "для идентификации"): https://github.com/settings/tokens
GITHUB_TOKEN=

# Порт, на котором запустится сайт
PORT=5000
